{{ $data := dict }}
{{ $url := "https://raw.githubusercontent.com/YuushaExa/ani/refs/heads/main/data/anime-1-400.json" }}
{{ with try (resources.GetRemote $url) }}
  {{ with .Err }}
    {{ errorf "Unable to get remote resource %s: %s" $url . }}
  {{ else with .Value }}
    {{ $data = . | transform.Unmarshal }}
  {{ else }}
    {{ errorf "Unable to get remote resource %s" $url }}
  {{ end }}
{{ end }}

{{ range $data }}

  {{ $content := dict "mediaType" "text/markdown" "value" .description  }}
  {{ $id := .id | default "Untitled" }}
  {{ $english_title := .title.english | default "Untitled" }}
  {{ $cover-large := .coverImage.large | default "Untitled" }}

  {{ $params := dict "image" $cover-large "mal" .idMal }}

  {{ $page := dict
    "content" $content
    "kind" "page"
    "params" $params
    "path" $id
    "title" $english_title
  }}
  {{ $.AddPage $page }}

{{ end }}
