{{- $index := slice -}}
{{- range .Pages -}}
  {{- $pageCount := len .Pages -}}
  {{- $entry := dict "t" .Title "u" (replace .RelPermalink "/" "") "c" $pageCount -}}
  {{- $index = $index | append $entry -}}
{{- end -}}
{{- $index | jsonify -}}
