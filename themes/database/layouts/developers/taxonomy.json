{{- $.Scratch.Add "index" slice -}}
{{- range .Pages -}}
  {{- $pageCount := len .Pages -}}
  {{- $url := .RelPermalink | replaceRE "^.*/([^/]+)/$" "$1" -}}
  {{- $.Scratch.Add "index" (dict "c" $pageCount "t" .Title "u" $url) -}}
{{- end -}}
{{- $.Scratch.Get "index" | jsonify -}}
